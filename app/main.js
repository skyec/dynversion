requirejs.config({
    "paths": {
      "jquery": "//code.jquery.com/jquery-2.1.4.min",
    }
});

// Start loading the main application file. Put all of your application logic in there.
requirejs(['coolstuff']);

